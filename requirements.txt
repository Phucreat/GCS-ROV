# CNC NExora GCS - Cross-Platform Requirements (Windows, Linux, macOS)
# ======================================================================

# ── Core GUI & 3D Engine ───────────────────────────────────────────────
PyQt6>=6.4.0
PyQt6-WebEngine>=6.4.0       # Trình phát WebRTC Camera siêu mượt
pyqtgraph>=0.13.0            # Hiển thị đồ thị đo đạc, đo độ sâu
PyOpenGL>=3.1.6              # Kết xuất 3D mô hình ROV
pyvista>=0.40.0              # Đồ họa PBR 3D Subsea Digital Twin
pyvistaqt>=0.11.0            # Tích hợp PyVista vào giao diện PyQt6
pybullet>=3.2.5              # Mô phỏng vật lý 6-DOF ROV
numpy>=1.24.0
scipy>=1.11.0                # Nội suy bản đồ đáy biển (Seafloor mapping)

# ── Hardware, Navigation & Telemetry ──────────────────────────────────
pymavlink>=2.4.37            # Giao thức MAVLink điều khiển ROV
pyserial>=3.5                # Kết nối cổng COM / USB Serial
pygame>=2.5.0                # Đọc tín hiệu tay cầm lái Gamepad/Joystick

# ── Video Streaming & Vision ──────────────────────────────────────────
opencv-python>=4.8.0         # Xử lý luồng hình ảnh camera & AR HUD
requests>=2.28.0             # REST API & cập nhật trực tuyến
pydantic>=2.0.0

# ── Voice & Audio Assistant (Optional) ────────────────────────────────
sounddevice>=0.4.6           # Phát âm thanh cảnh báo
edge-tts>=6.1.0              # Trợ lý giọng nói tiếng Việt AI
SpeechRecognition>=3.10.0    # Nhận diện lệnh giọng nói

# ── AI Object Detection (Optional) ────────────────────────────────────
# Cài đặt thêm nếu muốn bật AI phát hiện đối tượng dưới nước:
# pip install ultralytics